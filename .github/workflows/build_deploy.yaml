name: Build website and deploy

on:
  push

jobs:

  build-web-and-deploy:

    runs-on: ubuntu-22.04

    defaults:
      run:
        shell: bash -l {0}
  
    steps:
      
      - name: Checkout
        uses: actions/checkout@v4

      #- name: Install R
      #  uses: r-lib/actions/setup-r@v2

      - name: Install Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10' 

      - name: Install Python libraries - QUARTO
        run: |
          pip install jupyter nbformat
      
      - name: Add Repo
        run: |
          sudo add-apt-repository ppa:ubuntugis/ppa
          sudo apt-get update
      
      # for PGIS2
      - name: Install system libraries
        run: sudo apt-get install gdal-bin libgdal-dev python3-gdal python3-fiona python3-shapely python3-rasterio python3-geopandas

      - name: Setup Quarto
        uses: quarto-dev/quarto-actions/setup@v2
        
      - name: Install extensions
        run: ./install.sh  

      - name: Publish to GitHub Pages (and render)
        uses: quarto-dev/quarto-actions/publish@v2
        with:
          target: gh-pages
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}  